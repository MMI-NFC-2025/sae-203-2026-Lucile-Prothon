---
const { title, subTitle } = Astro.props;
---

<div>
    <div>
        <h1>{title}</h1>
        {
            subTitle && (
                <h2>
                    {subTitle}
                </h2>
            )
        }
    </div>
</div>
